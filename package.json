{
  "name": "ebi-project",
  "private": true,
  "scripts": {
    "dev:api": "python -m uvicorn api.index:app --reload --host 0.0.0.0 --port 8000",
    "deploy:hosting": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0"
  }
}
