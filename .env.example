# Local API only — copy to .env in project root
GEMINI_API_KEY=your-google-gemini-api-key
# Optional: force a single model (e.g. gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# Vercel: add GEMINI_API_KEY in Project → Settings → Environment Variables (Production)
